deployer:
  domain: apps.cluster-guid.example.com

tenant:
  user:
    name: USERNAME

# Namespace where the ArgoCD instance lives. Students deploy into this same
# namespace — no separate lab namespace required.
namespace: NAMESPACE-MUST-BE-SET-BY-CATALOG
