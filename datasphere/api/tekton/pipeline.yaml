apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: datasphere-api-pipeline
spec:
  params:
    - name: APP_NAME
      type: string
      default: datasphere-api
    - name: APP_GIT_URL
      type: string
      description: Gitea URL for the datasphere-api repository
      # This default is overridden at runtime with the student's Gitea URL
      default: https://gitea.apps.cluster.example.com/userX/datasphere-api.git
    - name: APP_GIT_REVISION
      type: string
      default: main
    - name: IMAGE
      type: string
      default: image-registry.openshift-image-registry.svc:5000/$(context.pipelineRun.namespace)/datasphere-api:latest

  workspaces:
    - name: source

  tasks:
    # ── 1. Clone source from Gitea ─────────────────────────────────────────
    - name: git-clone
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: git-clone
          - name: namespace
            value: openshift-pipelines
      params:
        - name: URL
          value: $(params.APP_GIT_URL)
        - name: REVISION
          value: $(params.APP_GIT_REVISION)
        - name: DELETE_EXISTING
          value: 'true'
      workspaces:
        - name: output
          workspace: source

    # ── 2. Build container image from Dockerfile using Buildah ────────────
    - name: build-image
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: buildah
          - name: namespace
            value: openshift-pipelines
      params:
        - name: IMAGE
          value: $(params.IMAGE)
        - name: DOCKERFILE
          value: ./Dockerfile
        - name: CONTEXT
          value: .
        - name: STORAGE_DRIVER
          value: vfs
        - name: TLS_VERIFY
          value: 'false'
        - name: FORMAT
          value: oci
      runAfter:
        - git-clone
      workspaces:
        - name: source
          workspace: source

    # ── 3. Update the running deployment to the new image ────────────────
    - name: deploy
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: openshift-client
          - name: namespace
            value: openshift-pipelines
      params:
        - name: SCRIPT
          value: >-
            oc set image
            deployment/$(params.APP_NAME)
            $(params.APP_NAME)=$(params.IMAGE)
            -n $(context.pipelineRun.namespace)
      runAfter:
        - build-image
